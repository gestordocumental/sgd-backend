apiVersion: v1
kind: Secret
metadata:
  name: document-service-secret
  namespace: gestor-documental
  labels:
    app: document-service
    project: sgd
type: Opaque
# IMPORTANTE: Solo para local. En producción usar Vault / Sealed Secrets.
# Generar: echo -n "valor" | base64
stringData:
  MONGODB_USERNAME: "document_user"
  MONGODB_PASSWORD: "document_pass_local"
  MINIO_ACCESS_KEY: "minio_admin"
  MINIO_SECRET_KEY: "minio_secret_local"
