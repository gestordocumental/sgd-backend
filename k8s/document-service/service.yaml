apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: gestor-documental
  labels:
    app: document-service
    project: sgd
spec:
  type: ClusterIP
  selector:
    app: document-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
