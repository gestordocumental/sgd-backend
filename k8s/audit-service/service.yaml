apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: gestor-documental
  labels:
    app: audit-service
    project: sgd
spec:
  type: ClusterIP
  selector:
    app: audit-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
