apiVersion: v1
kind: Secret
metadata:
  name: org-service-secret
  namespace: gestor-documental
  labels:
    app: org-service
    project: sgd
type: Opaque
# IMPORTANTE: Solo para local. En producción usar Vault / Sealed Secrets.
# Generar: echo -n "valor" | base64
stringData:
  DB_USERNAME: "org_user"
  DB_PASSWORD: "org_pass_local"
