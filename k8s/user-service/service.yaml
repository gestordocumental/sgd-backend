apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: gestor-documental
  labels:
    app: user-service
    project: sgd
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
