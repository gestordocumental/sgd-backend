apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: gestor-documental
  labels:
    app: workflow-service
    project: sgd
spec:
  type: ClusterIP
  selector:
    app: workflow-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
