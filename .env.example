# Variables para docker-compose (infraestructura local)
# Copiar a .env antes de levantar la infra:
#   cp .env.example .env

# Kong JWT Secret — DEBE coincidir con JWT_SECRET en services/auth-service/.env
KONG_JWT_SECRET=local-jwt-secret-change-me
